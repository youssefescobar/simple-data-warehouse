name: 'xyz_store_analytics'
version: '1.0.0'
profile: 'xyz_store_analytics'
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


models:
  xyz_store_analytics:
    # Config indicated by + and applies to all files under models/example/
    silver:
      +materialized: view
      +schema: silver
    gold:
      +materialized: table
      +schema: gold